#include<stdio.h>
main()
{
	float ar,s;
	printf("\nEnter the length of the side of a square : ");
	scanf("%f",&s);
	ar=s*s;
	printf("\nArea of the square for given value of side is : %0.3f ",ar);
	return 0;
}
