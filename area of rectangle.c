#include<stdio.h>
main()
{
	float ar,l,b;
	printf("\nEnter the length oand bredth of rectangle : ");
	scanf("%f%f",&l,&b);
	ar=l*b;
	printf("\nArea of the rectangle for give dimensions is : %0.3f ",ar);
	return 0;
}
