#include<stdio.h>
main()
{
	float per,l,b,r;
	printf("\nEnter the length and bredth of rectangle : ");
	scanf("%f%f",&l,&b);
	per=2*3.14*r*r;
	printf("\nperimeter of the rectangle for given dimensions is : %0.3f ",per);
	return 0;
}
